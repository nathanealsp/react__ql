{
  "name": "server",
  "version": "1.0.0",
  "description": "Express app to serve up the backend.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js",
    "nuke": "rm -rf node_modules && npm install"
  },
  "author": "Nathaneals",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^6.2.0",
    "express": "^4.16.4",
    "express-graphql": "^0.7.1",
    "graphql": "^14.1.1",
    "mongoose": "^5.4.11"
  }
}
